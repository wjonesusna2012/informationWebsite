{
  "name": "@info/schemas",
  "version": "1.0.0",
  "description": "Schemas for front and backend communications",
  "main": "dist/index.js",
  "author": "William Jones",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "zod": "^3.21.4"
  },
  "scripts": {
    "build": "tsc -p .",
    "clean": "rm -rf ./dist && tsc -p .",
    "lint": "eslint . --ext .ts",
    "watch": "yarn run build -- -watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^5.1.3"
  },
  "files": [
    "/dist/**/*"
  ]
}
